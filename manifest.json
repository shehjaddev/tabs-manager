{
  "manifest_version": 3,
  "name": "Tabs Manager",
  "version": "1.6.0",
  "description": "Tabs pinning, grouping, and duplication tool: toggle pin, pin/unpin all, group/ungroup unpinned tabs, and duplicate the current tab",
  "permissions": ["tabs"],
  "background": {
    "service_worker": "background.js"
  },
  "commands": {
    "toggle-pin": {
      "suggested_key": {
        "default": "Ctrl+Shift+X"
      },
      "description": "Toggle pin/unpin current tab"
    },
    "toggle-pin-all": {
      "suggested_key": {
        "default": "Ctrl+Shift+Y"
      },
      "description": "Pin all if unpinned tabs exist, else unpin all"
    },
    "group-unpinned": {
      "suggested_key": {
        "default": "Ctrl+Shift+Z"
      },
      "description": "Group all unpinned tabs or ungroup active group"
    },
    "duplicate-tab": {
      "suggested_key": {
        "default": "Shift+Alt+D"
      },
      "description": "Duplicate the current tab"
    }
  }
}
